# Dev/eval harness deps (the scored image uses only agent/requirements.txt).
fastapi>=0.110          # eval/mock_server
uvicorn[standard]>=0.29 # serves the mock during eval/harness
httpx>=0.27             # agent's Fireworks client
python-dotenv>=1.0      # agent/config .env loading
